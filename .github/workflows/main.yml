name: Instagram Signup on RDP + Record

on:
  workflow_dispatch:

jobs:
  rdp-record:
    runs-on: windows-latest
    timeout-minutes: 20

    env:
      RDP_USERNAME: runneradmin
      RDP_PASSWORD: "RdpAccess#2026X"

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Configure RDP
        shell: powershell
        run: |
          $ErrorActionPreference = "Stop"
          $username = $env:RDP_USERNAME
          $password = $env:RDP_PASSWORD
          $securePassword = ConvertTo-SecureString $password -AsPlainText -Force
          $existingUser = Get-LocalUser -Name $username -ErrorAction SilentlyContinue
          if ($null -ne $existingUser) {
              Set-LocalUser -Name $username -Password $securePassword -ErrorAction Stop
          } else {
              New-LocalUser -Name $username -Password $securePassword -PasswordNeverExpires -UserMayNotChangePassword -Description "Temp RDP" -ErrorAction Stop
          }
          $adminMember = Get-LocalGroupMember -Group "Administrators" -ErrorAction SilentlyContinue | Where-Object { $_.Name -match "\\$([regex]::Escape($username))$" }
          if ($null -eq $adminMember) { Add-LocalGroupMember -Group "Administrators" -Member $username -ErrorAction Stop }
          $rdpMember = Get-LocalGroupMember -Group "Remote Desktop Users" -ErrorAction SilentlyContinue | Where-Object { $_.Name -match "\\$([regex]::Escape($username))$" }
          if ($null -eq $rdpMember) { Add-LocalGroupMember -Group "Remote Desktop Users" -Member $username -ErrorAction Stop }
          Set-ItemProperty -Path "HKLM:\System\CurrentControlSet\Control\Terminal Server" -Name "fDenyTSConnections" -Value 0 -Force
          Set-ItemProperty -Path "HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp" -Name "UserAuthentication" -Value 1 -Force
          Get-NetFirewallRule -DisplayGroup "Remote Desktop" -ErrorAction SilentlyContinue | Enable-NetFirewallRule -ErrorAction SilentlyContinue
          Set-Service -Name "TermService" -StartupType Automatic -ErrorAction Stop
          Restart-Service -Name "TermService" -Force -ErrorAction Stop
          Start-Sleep -Seconds 4
          Write-Host "RDP configured."

      - name: Install and Start Tailscale
        shell: powershell
        env:
          TAILSCALE_AUTH_KEY: ${{ secrets.TAILSCALE_AUTH_KEY }}
        run: |
          $ErrorActionPreference = "Stop"
          $tailscaleExe = "C:\Program Files\Tailscale\tailscale.exe"
          if (-not (Test-Path $tailscaleExe)) {
              $installer = "$env:TEMP\tailscale-setup.exe"
              Invoke-WebRequest -Uri "https://pkgs.tailscale.com/stable/tailscale-setup-latest.exe" -OutFile $installer
              Start-Process -FilePath $installer -ArgumentList "/quiet" -Wait
              Start-Sleep -Seconds 5
          }
          & $tailscaleExe up --auth-key="$env:TAILSCALE_AUTH_KEY" --hostname="github-actions-rdp-3" --accept-dns=false
          if ($LASTEXITCODE -ne 0) { throw "Tailscale failed." }
          Start-Sleep -Seconds 5
          $tailscaleIP = (& $tailscaleExe ip -4 | Out-String).Trim()
          Write-Host "Tailscale IP: $tailscaleIP"

      - name: Install Chrome
        shell: powershell
        run: |
          $ErrorActionPreference = "Stop"
          $paths = @(
            "C:\Program Files\Google\Chrome\Application\chrome.exe",
            "C:\Program Files (x86)\Google\Chrome\Application\chrome.exe"
          )
          $chromePath = $null
          foreach ($p in $paths) { if (Test-Path $p) { $chromePath = $p; break } }
          if (-not $chromePath) {
              choco install googlechrome -y --no-progress
              Start-Sleep -Seconds 6
              foreach ($p in $paths) { if (Test-Path $p) { $chromePath = $p; break } }
          }
          if (-not $chromePath) { throw "Chrome not found." }
          "CHROME_PATH=$chromePath" | Out-File "$env:GITHUB_ENV" -Encoding utf8
          Write-Host "Chrome: $chromePath"

      - name: Prepare Chrome profile
        shell: powershell
        run: |
          New-Item -ItemType Directory -Path "C:\temp" -Force | Out-Null
          Get-Process chrome -ErrorAction SilentlyContinue | Stop-Process -Force -ErrorAction SilentlyContinue
          Start-Sleep -Seconds 2
          Remove-Item "C:\temp\ChromeProfile" -Recurse -Force -ErrorAction SilentlyContinue
          New-Item -ItemType Directory -Path "C:\temp\ChromeProfile" -Force | Out-Null
          Write-Host "Profile ready."

      - name: Minimize all windows
        shell: powershell
        run: |
          try {
              $shell = New-Object -ComObject Shell.Application
              $shell.MinimizeAll()
          } catch { Write-Host "MinimizeAll failed: $($_.Exception.Message)" }
          Start-Sleep -Seconds 2

      - name: Start Chrome
        shell: powershell
        run: |
          powershell.exe -NoProfile -ExecutionPolicy Bypass -File "$env:GITHUB_WORKSPACE\start-chrome.ps1"
          if ($LASTEXITCODE -ne 0) { throw "start-chrome failed with code $LASTEXITCODE." }

      - name: Install FFmpeg
        shell: powershell
        run: |
          $ffmpeg = Get-Command ffmpeg.exe -ErrorAction SilentlyContinue
          if (-not $ffmpeg) {
              choco install ffmpeg -y --no-progress
              Start-Sleep -Seconds 5
          }
          $ffmpeg = Get-Command ffmpeg.exe -ErrorAction SilentlyContinue
          if (-not $ffmpeg) { throw "FFmpeg not found." }
          Write-Host "FFmpeg: $($ffmpeg.Source)"

      - name: Run recording
        shell: powershell
        run: |
          powershell.exe -NoProfile -ExecutionPolicy Bypass -File "$env:GITHUB_WORKSPACE\record.ps1"
          if ($LASTEXITCODE -ne 0) { throw "Recording failed with code $LASTEXITCODE." }

      - name: Debug outputs
        if: always()
        shell: powershell
        run: |
          Write-Host "===== click-record.log ====="
          if (Test-Path "C:\temp\click-record.log") { Get-Content "C:\temp\click-record.log" }
          else { Write-Host "click-record.log NOT FOUND" }
          Write-Host ""
          Write-Host "===== ffmpeg-error.log ====="
          if (Test-Path "C:\temp\ffmpeg-error.log") { Get-Content "C:\temp\ffmpeg-error.log" }
          else { Write-Host "ffmpeg-error.log NOT FOUND" }

      - name: List recording files
        if: always()
        shell: powershell
        run: |
          Write-Host "=== C:\temp directory ==="

          if (Test-Path "C:\temp") {
              Get-ChildItem "C:\temp" -Force |
                Select-Object Name, Length, LastWriteTime |
                Format-Table -AutoSize

              Write-Host ""
              Write-Host "=== Required files ==="

              $files = @(
                "C:\temp\rdp-click-video.mp4",
                "C:\temp\rdp-click-screenshot.png",
                "C:\temp\click-record.log",
                "C:\temp\ffmpeg-error.log",
                "C:\temp\ffmpeg-output.log"
              )

              foreach ($file in $files) {
                  if (Test-Path $file) {
                      $item = Get-Item $file
                      Write-Host "FOUND: $file ($($item.Length) bytes)"
                  } else {
                      Write-Host "MISSING: $file"
                  }
              }
          }
          else {
              Write-Host "C:\temp DOES NOT EXIST"
          }

      - name: Upload artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: instagram-rdp-record
          path: |
            C:\temp\rdp-click-video.mp4
            C:\temp\rdp-click-screenshot.png
            C:\temp\click-record.log
            C:\temp\ffmpeg-output.log
            C:\temp\ffmpeg-error.log
            C:\temp\instagram_signup_filled.png
            C:\temp\instagram_captcha_modal.png
            C:\temp\instagram_captcha_clicked.png
            C:\temp\error_missing_element.png
          if-no-files-found: warn
          retention-days: 7
